修复部分公众号历史文章无法完整读取的问题，并完善中断恢复体验。

- 兼容微信当前的作者文章列表，自动识别 `author_id`。
- 使用 `from_article_id` 游标连续读取后续页面。
- 旧历史接口首屏为空时自动切换新链路，不再误报 JSON 错误或把“0 篇”当成读取完成。
- 网页内备用下载面板同步兼容新链路。
- 读取异常时保留已发现的文章和进度，并提供继续操作提示。
- 诊断日志不再输出文章列表会话 Cookie。

现场链路验证中，同一公众号的旧接口返回 0 篇，新接口第一页返回 7 篇，并在下一页正确结束。Go 全量测试、Swift 测试、JavaScript 语法检查、应用签名校验和发布包解压校验均已通过。

适用于 Apple Silicon Mac，最低 macOS 14。公共安装包使用 ad-hoc 签名，尚未经过 Apple 公证。

SHA-256：`462a868e0477fd9d4dc2247245c0cc7d84359a73b6acfb3e7b78ed3b08145924`
