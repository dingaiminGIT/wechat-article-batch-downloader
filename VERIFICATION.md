# 公众号文章下载器实机验证

更新时间：2026-09-14 22:55（本机 Apple Silicon Mac）。

## 1.0.2 证书兼容修复

| 检查 | 实际结果 |
|---|---|
| v1.0.1 反证 | 在隔离 HOME 中预置无效的 `~/.mitmproxy/mitmproxy-ca-cert.pem` 与 `mitmproxy-ca.pem`，从公开 v1.0.1 ZIP 解压出的后端错误选择旧文件，代理因 `failed to decode certificate PEM` 退出 |
| 显式路径优先 | 同一隔离场景使用修复后二进制，成功在配置指定的 `~/.config/mp-article-batch-downloader/certs/` 生成 `root-ca.pem` 与 `root-ca-key.pem`，API 和代理均启动 |
| 私钥权限 | 发布验证脚本确认新私钥仅当前用户可读写，未向组或其他用户开放权限 |
| 旧文件保护 | 测试只读取伪造的 `~/.mitmproxy` 作为冲突条件，不会重命名、覆盖或删除用户已有证书 |
| Go 回归测试 | `internal/config` 和 `pkg/certificate` 新增显式路径、旧 mitmproxy 残留、自定义路径复用及残缺证书对测试；完整相关 Go 测试通过 |
| Swift 构建测试 | macOS SwiftPM 构建完成，现有 6 项测试全部通过；证书信任域改为当前用户，不再需要管理员密码 |
| 成品门禁 | `package_release.sh` 解压生成的 ZIP、校验代码签名，并对 ZIP 内后端再次运行证书优先级验证 |

发布验证入口：`python3 script/verify_certificate_precedence.py /path/to/mp_article_batch_downloader`。

## 2026-09-13 自动识别回归验证

- 测试对象为此前未出现在新应用左侧的“南方都市报”。
- 连接微信后只重新打开文章，没有点击网页右侧的“读取文章”或“批量下载”。
- 新应用左侧公众号数量由5变为6，并显示“南方都市报 · 最近已连接”。
- 自动化回归脚本验证文章页即使找不到旧面板依赖的 DOM 节点，也会主动调用公众号凭证同步接口。

## 此前 1.0.0 管理员信任修复记录

用户输入管理员密码后仍停留在“授权并连接”。捕获到的原始错误是：

> SecTrustSettingsSetTrustSettings: The authorization was denied since no user interaction was possible.

原实现通过后台管理员 AppleScript 运行证书信任命令，后续信任设置所需的交互被系统拒绝；应用又把真实错误替换成泛化提示。现改为 GUI 应用进程内调用 Security.framework 的 SecTrustSettingsSetTrustSettings，系统授权由用户在原生窗口完成。失败原因保存在 connection.log 并显示在界面。

实现依据：[Apple SecTrustSettingsSetTrustSettings 文档](https://developer.apple.com/documentation/security/sectrustsettingssettrustsettings(_:_:_:))，该接口会请求系统认证，并可能等待用户交互。

## 2026-09-12 本轮通过的实机检查

| 检查 | 实际结果 |
|---|---|
| 用户完成原生系统授权 | connection.log 记录 authorize=true、exit=0；security verify-cert 返回 certificate verification successful |
| 网络连接 | 系统 Wi-Fi 的 HTTP 与 HTTPS 代理均为 127.0.0.1:2133，新服务收到微信文章请求 |
| 公众号识别 | AI白牙在新服务中变为有效连接，应用显示“已连接微信” |
| 历史读取 | 在真实文章页点击“读取文章”，历史列表10篇加当前页1篇，共显示11篇；当前页稳定出现在第11项 |
| 真实批量下载 | 原10条任务全部done；漏掉的《如何将 Agent 等消息触达到微信？》已补为第11篇，HTML、Markdown、TXT均存在 |
| 原失败长文 | “AI时代再穷不能穷模型，再省不能省Token”本次成功，TXT 17,642 字节 |
| 新应用下载中心 | Computer Use看到6个公众号目录；AI白牙新目录显示11篇及最近下载时间20:21，旧目录显示10篇及19:54 |
| 新应用打开目录 | 实际点击AI白牙的“打开目录”，Finder准确打开`~/Downloads/公众号文章归档/AI白牙` |
| 微信文章页入口 | ego-browser真实加载目标文章，批量面板按钮已改为“打开下载目录”，点击后出现“已打开‘AI白牙’下载目录” |
| 当前页防漏 | ego-browser读取后状态为“已读取：11篇”，并验证列表包含《如何将 Agent 等消息触达到微信？》 |
| 微信分页兼容 | 微信对“快刀青衣”每页均返回 `can_msg_continue=0`，但 `next_offset` 持续前进；现按实际偏移和新文章判断是否继续，不再停在首批10篇 |
| 全部历史实测 | 原生应用实际读取“快刀青衣”31页，最终状态complete，共297篇；第30页仍有5篇，第31页空页后正常结束 |
| 早期文章链接 | 支持旧式`/mp/appmsg/show`链接的`appmsgid/itemidx/sign`稳定标识；最早6篇不再被错误合并，界面显示“全部历史”和“下载全部297篇” |
| 网页读取范围 | ego-browser真实加载新版面板，默认选项为“全部历史”，并提供最近10/20/50页；达到范围上限时明确提示后面还有文章 |
| 无终端运行 | 已停止并关闭旧命令行版终端；新应用后台标准输出和错误输出均写入`~/Library/Application Support/MPArticleDownloader/service.log`，不创建终端窗口 |
| 旧记录 | 仍保留262条旧任务缓存；255条有本机TXT文件。旧记录只读接入，不把旧任务的暂停/重试发给新服务 |
| 公众号页已有下载 | 有本机文件时直接提供“打开公众号目录”，不再展开单篇正文记录 |
| 批次进度 | 下载中心实机显示 `AI白牙 11/11`；快刀青衣显示 `0/297`，并分别标明暂停234、文件缺失63 |
| 执行时间 | 每个新批次记录开始时间；运行中每秒更新耗时，结束后固定显示结束时间和总耗时。实机显示AI白牙总耗时19分35秒 |
| 公众号目录名 | 下载中心只显示“快刀青衣”；新任务和实际目录均使用公众号名，不再追加biz字符串。实机打开后目录为`~/Downloads/公众号文章归档/快刀青衣` |
| 重复授权 | 已信任证书时再次点击连接，`connection.log`记录`authorize=false exit=0`并直接变为“已连接微信”，没有再次弹出密码窗口 |
| 稳定应用身份 | 安装包改用本机Apple Development身份签名，TeamIdentifier为`SFSP8H8MTX`；后续本地更新保持同一身份，减少macOS重复询问目录权限 |
| 大批失败诊断 | 原实现对每篇文章先解析、再导出，各请求一次正文；297篇任务近似产生双倍正文请求并触发微信访问验证。现解析结果直接传给导出，每篇只请求一次，并将正文请求按每秒1次串行化 |
| 下载前检查 | 批量入队前先探测所选范围内最早的一篇；若微信凭证失效或触发访问验证，不再先创建数百个必然失败的任务 |
| 快刀青衣续传 | 原批次从43/297、失败254续跑到294/297；HTML、Markdown、TXT各294份。剩余3篇均由微信明确返回“此内容因违规无法查看” |
| JSONL并发写入 | 修复多任务同时读写`style_corpus.jsonl`造成记录覆盖和内容交错；写入改为进程内串行并通过同目录临时文件原子替换。当前索引已重建并验证为294条、294个唯一URL |
| 图片导出性能 | 移除解析阶段对每张图片的额外HEAD探测；Markdown先保存图片，自包含HTML复用同一份内存或本机图片。同一篇文章、同一图片由最多3次网络访问降为1次 |
| JSONL追加性能 | 新文章改为直接追加一条JSONL，只有重复URL更新时才原子重写索引，避免每下载一篇都重新解析并写回整个语料文件 |
| 优化前后实测 | 删除“每天晒白牙”原210篇目录和任务后，以相同210篇重新完整下载：由4分35秒降至3分35秒，节省1分钟（约22%）；210/210成功、失败0 |
| 优化后文件核对 | “每天晒白牙”HTML、Markdown、TXT、JSONL各210份/条，JSONL含210个唯一URL；本地图片847份，目录371MB |
| 安全/快速模式 | 设置页和下载前操作区均提供速度选择，默认安全模式每秒最多1篇；快速模式约每秒2篇，并把模式写入批次记录 |
| 快速模式保护 | 首轮快速实测在第106篇遇到一次短暂访问验证；现会自动按安全间隔重试，连续失败才暂停同批次剩余任务。错误文案的两种实际形式均有单元测试覆盖 |
| 快速模式完整实测 | 再次删除“每天晒白牙”目录和210条任务后，以快速模式完整重下：210/210成功、失败0，总耗时1分53秒，平均110.7篇/分钟；相对安全模式3分35秒缩短约47% |
| 快速模式文件核对 | “每天晒白牙”HTML、Markdown、TXT、JSONL各210份/条，JSONL为210个唯一URL，本地图片847份，目录371MB；《如何将 Agent 等消息触达到微信》存在于第4篇 |
| 下载中心交互 | 实机显示“每天晒白牙 210/210篇、已完成、快速模式、开始22:42、结束22:44、总耗时1分53秒、平均110.7篇/分钟”，并保留单一“打开目录”动作 |
| 公众号页交互 | 实机显示“尚未读取 · 本机已保存210篇”，读取范围使用最近/全部/日期三段选择；无读取结果时只显示一个“打开公众号目录”，不展开正文记录 |

目录统计只读取公众号目录下HTML/TXT文件名和目录修改时间，不递归扫描图片资源或其他下载路径，避免下载中心卡顿。

## 安装与数据

- 安装应用：`~/Applications/公众号文章下载器.app`
- 新服务 API / 代理端口：2132 / 2133
- 应用数据：`~/Library/Application Support/MPArticleDownloader/`
- 本轮下载：`~/Downloads/公众号文章归档/AI白牙/`
- 旧命令行服务已停止；当前微信连接使用新应用2133。
- 旧记录缓存仅保存展示元数据与文件路径，不保存原请求URL或微信凭证。

## 其他验证

- Swift 6项测试通过，覆盖稳定文件名、批次进度/耗时、完成状态、扫描解码、缓存排除凭证和文件删除后状态更新。
- Go新增并发写入测试，40个任务同时追加后保留40条唯一JSONL记录，并覆盖重复URL替换、HTML/Markdown图片只请求一次、快速模式间隔和访问验证自动重试/暂停分类；微信访问验证与违规/删除页面分类测试通过。
- Go分页归一化、旧式文章链接、扫描和接口测试通过；JavaScript语法检查、应用构建安装、本地签名和git diff --check通过。
- 此前Go扫描/队列/证书/代理相关测试（含race）、本地文章服务器下载集成测试、父进程退出回收测试通过，入口见DESKTOP.md。
- 此前真实渲染正文导出已生成HTML、Markdown、TXT、JSONL及10张本地图片，文件位于“应用验证-真实文章”目录。

## 验证边界

本轮实机验证对象包括AI白牙11篇的当前页防漏，以及快刀青衣297篇的完整历史分页和续传。快刀青衣可下载的294篇均已落盘；其余3篇为微信上游限制内容，应用会保留明确失败状态。其他公众号需要各自有效的微信连接。应用使用本机Apple Development身份签名，未做Developer ID签名、公证或外部发行。
