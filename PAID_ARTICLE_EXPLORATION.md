# 付费文章下载探索

## 目标

在不绕过公众号付费机制的前提下，探索付费文章的导出能力：

1. 使用工具的用户已经合法付费，工具应能导出该用户可见的全文。
2. 使用工具的用户没有付费，工具不能绕过付费墙下载全文；只能识别受限状态、保存预览内容，并提示需要在微信内购买或登录已购买账号后再导出。

## 当前结论

现有批量下载流程只把文章 URL 交给后台：

`officialaccount.js` -> `/api/task/create2` -> `officialaccount://文章URL` -> Go 后台重新请求文章页 -> 解析 `window.cgiDataNew.content_noencode` -> 导出 HTML/Markdown/TXT。

付费文章未解锁时，微信页面返回的 `content_noencode` 只包含预览片段，并在预览末尾出现类似：

```html
<mp-pay-preview-filter data-offset="11"></mp-pay-preview-filter>
```

因此当前工具保存的是微信实际返回的预览内容，不是下载流程中途丢失正文。

## 已付费用户的可行路线

当前分支同时保留两条路线。

### 路线一：后台复用已授权凭证

这是优先路线，目标是尽量保留批量下载价值。

1. 前端刷新公众号凭证时，除 `uin`、`key`、`pass_ticket`、`appmsg_token` 外，也提交页面脚本能读到的 `document.cookie`。
2. 后台保存这些凭证到本地 `mp.json`；该文件已被 `.gitignore` 忽略。
3. 公众号文章下载协议在请求文章 URL 前，根据 `__biz` 查询本地已刷新凭证。
4. 请求文章页时附加 `uin`、`key`、`pass_ticket` 等参数，并优先使用该公众号对应 cookie。
5. 通过只读探测接口判断返回内容是 `full` 还是 `preview`，不输出正文和敏感凭证。

关键待验证点：微信付费阅读态是否只依赖这些后台可复用凭证。如果付费态绑定在 WebView 内部、HttpOnly cookie、临时授权或逐篇阅读会话上，这条路线可能仍然只能拿到预览。

### 路线二：当前页面导出

这是兜底路线：

1. 用户在微信内置浏览器中打开自己已付费、已能正常阅读全文的文章。
2. 注入脚本从当前页面读取 `window.cgiDataNew.content_noencode`，或从当前 DOM 的正文容器读取已经渲染出的完整正文。
3. 前端把标题、作者、发布时间、正文 HTML、图片/视频元信息和原文 URL 发给本地后端。
4. 后端复用现有 HTML/Markdown/TXT 导出逻辑，直接从前端传入的正文生成文件，不再重新请求文章 URL。
5. 如果正文仍包含 `mp-pay-preview-filter` 或长度明显短于页面声明的全文长度，标记为“仅预览”，不要伪装成全文成功。

这个路线的关键点是：导出的内容来自用户当前已授权可见的页面，而不是尝试构造或绕过未授权请求。

## 未付费用户的边界

不能实现“未付费下载全文”。这属于绕过付费墙，工具不应提供。

可实现的合规能力：

1. 自动检测 `mp-pay-preview-filter`、`js_pay_preview_filter` 等付费预览标记。
2. 在下载结果、任务状态或文件元数据中注明“仅保存预览内容”。
3. 给出明确提示：需要在微信中用已购买账号打开全文后，再使用“当前页面导出”。
4. 保留当前行为：未付费时可以导出试看片段，便于用户做索引或记录。

当前安全探测结果：

1. 直接请求付费文章 URL，返回页面仍包含 `mp-pay-preview-filter` / `pay_preview` 标记。
2. 给文章 URL 附加已刷新到本机的 `uin`、`key`、`pass_ticket` 参数后，返回页面仍包含付费预览标记。
3. 现阶段未发现仅靠公开 URL 参数即可绕过付费预览的漏洞。
4. 如果后续发现未付费状态能返回完整 `content_noencode`，应停止下载实现，保留最小复现证据，并按微信安全响应渠道提交。

## 实现清单

### 后端

1. 在公众号导出包中新增一个“从已解析文章对象导出”的方法，复用现有 `BuildHTMLFromArticle`、`ConvertHtmlToMarkdownFile` 和纯文本导出逻辑。
2. 新增 API，例如 `POST /api/mp/article/export_current`，接收当前页面采集到的文章数据。
3. 在后端检测付费预览标记，并把导出结果返回为 `full` 或 `preview`。
4. 新增只读探测 API，例如 `POST /api/mp/article/probe`，用于判断后台复用凭证后是否拿到全文；该接口不得返回正文或敏感凭证。

### 前端注入脚本

1. 在文章详情页新增“导出当前页”入口，区别于现有“按 URL 下载”。
2. 读取 `window.cgiDataNew` 中的标题、作者、发布时间、正文 HTML 和图片/视频信息。
3. 如果 `window.cgiDataNew.content_noencode` 不完整，尝试读取当前正文 DOM；仍不完整则提示“当前页面仅有预览内容”。
4. 调用后端导出 API，并在面板中展示导出状态。
5. 刷新公众号凭证时提交页面脚本可读 cookie，用于后台授权抓取实验；如果微信把关键付费态放在 HttpOnly cookie 里，前端脚本无法读取，这是预期限制。

### 验证

1. 普通免费文章：当前页导出应与原 URL 下载内容基本一致。
2. 未付费文章：导出结果应明确标记为预览，不应声称成功下载全文。
3. 已付费文章：用户页面能看到全文时，应导出完整正文。
4. 批量下载：仍按 URL 下载免费文章；付费文章可先作为预览记录，后续再考虑逐篇打开页面后采集。
